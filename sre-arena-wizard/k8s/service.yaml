apiVersion: v1
kind: Service
metadata:
  name: wizard-api
  namespace: sre
spec:
  type: ClusterIP
  selector:
    app: wizard-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
